<!-- # stack-ts

>A stack data structure written in [typescript](https://www.typescriptlang.org/)

## Usage

To install the package run:

```bash
npm install stack-ts
```
Then, to instantiate a new stack in your project:
```js
const Stack = require('stack-ts').Stack;
const stack = new Stack();
```

### To Run Tests
```bash
npm test
```

## Methods

### Push
Add an item to the top of the stack.
```js
stack.push({ property: "value" });
``` -->
